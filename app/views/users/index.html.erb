<h2>Users</h2>

<% @users.each do |user| %>
  <li>
    <%= user.name %>
    <%= link_to '💌', conversations_path(sender_id: current_user.id, recipient_id: user.id), method: :post %>
  </li>
<% end %>